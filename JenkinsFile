pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                script {
                    // Check out the code from the repository
                    checkout([$class: 'GitSCM', branches: [[name: '*/main']], userRemoteConfigs: [[url: 'https://github.com/huzaifa-123/SPM-Semester-Project.git']]])
                }
            }
        }

        stage('Build and Deploy') {
    steps {
        script {
            // Assuming your website is static content (HTML, CSS, JS)
            // Copy the files to a web server directory
            echo 'Copying files to the web server directory'
            
            // Use 'copy' command on Windows, and 'cp' on Unix-like systems
            if (isUnix()) {
                sh 'cp -r * /var/www/html/'
            } else {
                bat 'copy * C:\\path\\to\\your\\web\\server\\directory\\'
            }
        }
    }
}

    }

    post {
        always {
            // Clean up or perform any other actions after the pipeline
            echo 'CI Pipeline completed!'
        }
    }
}
